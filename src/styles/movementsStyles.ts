import { StyleSheet } from 'react-native';

export const movementsStyles = StyleSheet.create({
    areaViewHeader: {
        marginTop: 26,
    },
    container: {
        padding: 20,
    },
    titleMovement : {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 20,
    },
    buttonMovements: {
        marginBottom: 30,
        marginTop: 20,
    }
});