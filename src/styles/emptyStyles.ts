import { StyleSheet } from "react-native";

export const emptyStyles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontSize: 20,
        color: '#666',
        textAlign: 'center',
    }
});