import { StyleSheet } from "react-native";

export const emptyStyles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        paddingVertical: 200,
    },
    text: {
        fontSize: 20,
        color: '#666',
        textAlign: 'center',
    }
});